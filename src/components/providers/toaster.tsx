/**
 * Re-export нашего кастомного `<Toaster />`.
 *
 * Раньше здесь жил Sonner-обёрнутый компонент: глобальная очередь
 * управлялась библиотекой sonner. Мы её заменили собственной
 * реализацией (см. `@/components/ui/toast`), потому что нужно было:
 *   1) стэкование «горкой» с раздвиганием на hover;
 *   2) animated entry/exit через `motion/react` (а не CSS-перехват
 *      sonner-овских `--y` переменных);
 *   3) точное соответствие нашему дизайн-референсу
 *      (тёмная карточка → светлая в light-теме, action-кнопки двух
 *      «весов» в зависимости от типа тоста);
 *   4) полный контроль над API (`toast.success/error/warning/info`
 *      теперь живут в одном модуле с UI и не зависят от внешней
 *      библиотеки).
 *
 * `Providers` импортирует `Toaster` отсюда — путь сохранён, чтобы
 * миграция была прозрачной для всех точек подключения.
 */
export { Toaster } from "@/components/ui/toast";
